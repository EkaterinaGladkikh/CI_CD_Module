pipeline {
    agent any 
    stages {
        stage('install req') { 
            withPythonEnv('python3') {
                sh 'pip install -r requirements.txt' 
            }
        }
        stage('run tests') { 
            withPythonEnv('python3') {
                sh 'pytest main.py' 
            }
        }
    }
}
